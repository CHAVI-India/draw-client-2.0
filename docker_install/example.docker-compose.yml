services:
  db:
    image: postgres:17
    container_name: draw-client-postgres-docker
    ports:
      - "5433:5432" # External port for postgres may need to be changed.
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
  rabbitmq:
    image: rabbitmq:3.12-management
    container_name: draw-client-rabbitmq-docker
    restart: unless-stopped
    ports:
      - "5675:5672" # External port for rabbitmq may need to be changed.
      - "15675:15672" # External port for rabbitmq management may need to be changed.
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest

  django-web:
    image: public.ecr.aws/g0w3c0z2/draw-client-2.0:latest
    container_name: draw-client-django-docker
    depends_on:
      db:
        condition: service_healthy
      memcached:
        condition: service_started
    env_file:
      - .env
    ports:
      - "11112:11112"  # DICOM SCP port - for receiving DICOM files from modalities/PACS
    volumes:
      - app_data:/app
      - ./logs:/app/logs
      - ./staticfiles:/app/staticfiles      
      - "dicomdata:/app/datastore"
      - ./certs:/usr/local/share/ca-certificates:ro
    command: ["./entrypoint.docker.sh"]
    restart: unless-stopped
    environment:
      - DOCKER_CONTAINER=true
    ulimits:
      nofile:
        soft: 65536
        hard: 65536

  celery:
    image: public.ecr.aws/g0w3c0z2/draw-client-2.0:latest
    container_name: draw-client-celery-docker
    command: ["./entrypoint.docker.sh", "celery"]
    volumes:
      - app_data:/app
      - ./logs:/app/logs
      - ./staticfiles:/app/staticfiles      
      - "dicomdata:/app/datastore"
      - ./certs:/usr/local/share/ca-certificates:ro
    env_file:
      - .env
    environment:
      - DOCKER_CONTAINER=true
    restart: unless-stopped      
    depends_on:
      db:
        condition: service_healthy
      django-web:
        condition: service_started
      rabbitmq:
        condition: service_started
    ulimits:
      nofile:
        soft: 65536
        hard: 65536

  celery-beat:
    image: public.ecr.aws/g0w3c0z2/draw-client-2.0:latest
    container_name: draw-client-celery-beat-docker
    command: ["./entrypoint.docker.sh", "celery-beat"]
    volumes:
      - app_data:/app
      - ./logs:/app/logs
      - ./staticfiles:/app/staticfiles      
      - "dicomdata:/app/datastore"
      - ./certs:/usr/local/share/ca-certificates:ro
    env_file:
      - .env
    environment:
      - DOCKER_CONTAINER=true
    depends_on:
      db:
        condition: service_healthy
      django-web:
        condition: service_started
      rabbitmq:
        condition: service_started
    restart: unless-stopped    
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
  memcached:
    image: 'memcached:latest'
    container_name: 'draw-client-memcached-docker'    
    volumes:
      - app_data:/app
      - ./logs:/app/logs
      - ./staticfiles:/app/staticfiles      
      - "dicomdata:/app/datastore"
      - ./certs:/usr/local/share/ca-certificates:ro    
    restart: unless-stopped    
    ports:
      - "11212:11211" # External port for memcached may need to be changed.
  frontend-proxy:
    image: nginx:latest
    container_name: draw-client-nginx-docker
    restart: unless-stopped
    ports:
      - "8002:80" # Application will be available on port 8002 on the host.
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./staticfiles:/staticfiles:ro
    depends_on:
      - django-web


# Uncomment one of the two following sections and fill in the parameters
# Use this section if you are using shared network storage
# volumes:
#   postgres_data:
#   app_data:
#   dicomdata:
#     driver_opts:
#       type: cifs
#       o: "username=${NETWORK_USER},domain=${NETWORK_DOMAIN},password=${NETWORK_PASSWORD},rw,uid=1000,gid=1000,file_mode=0660,dir_mode=0770"
#       device: ${NETWORK_PATH}  

# Use this section if you are using local storage. In the example below the D:/dicomdata is the path to the datastore on the local computer.
# volumes:
#   postgres_data:
#   app_data:
#   dicomdata:
#     driver: local
#     driver_opts:
#       o: bind
#       type: none
#       device: "D:/dicomdata"    